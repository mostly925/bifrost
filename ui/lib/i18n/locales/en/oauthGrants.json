{
	"title": "OAuth Grants",
	"subtitle": "Active downstream OAuth grants issued to MCP clients that connected via the OAuth consent flow.",
	"searchAria": "Search grants",
	"searchPlaceholder": "Search client, identity...",
	"loadError": "Failed to load OAuth grants: {{error}}",
	"toast": {
		"revoked": "Grant revoked",
		"revokeFailed": "Failed to revoke grant"
	},
	"actions": {
		"menuAria": "Grant actions",
		"viewAuthSessions": "View auth sessions",
		"revoke": "Revoke"
	},
	"columns": {
		"client": "Client",
		"boundTo": "Bound to",
		"boundToTooltip": "The identity this grant is tied to: an end user (via SSO), a virtual key (shared by anyone using that VK), or an anonymous session. This determines which upstream per-user OAuth sessions are reachable under this grant.",
		"tokenExpiry": "Access token expiry",
		"tokenExpiryTooltip": "When the current JWT access token expires. MCP clients silently refresh using the refresh token, so an active grant past its expiry will mint a new token automatically on the next request.",
		"created": "Created",
		"lastUsed": "Last used",
		"lastUsedTooltip": "When this grant last refreshed its access token. MCP clients refresh as their token nears expiry, so this tracks the grant's most recent activity. Grants that have not refreshed since they were authorized fall back to when they were created."
	},
	"expiry": {
		"unknown": "Unknown",
		"refreshOnNextUse": "Refreshes on next use",
		"inMinutes": "in {{count}} min"
	},
	"time": {
		"justNow": "just now",
		"minutesAgo": "{{count}}m ago",
		"hoursAgo": "{{count}}h ago",
		"daysAgo": "{{count}}d ago"
	},
	"table": {
		"noMatches": "No grants match these filters.",
		"range": "{{from}}-{{to}} of {{total}} entries",
		"previousAria": "Previous page",
		"nextAria": "Next page",
		"page": "Page",
		"of": "of {{count}}"
	},
	"emptyState": {
		"text": "No grants yet. Grants appear here when an MCP client connects via the OAuth consent flow. (Authentication Mode needs to be set to \"oauth\" or \"both\" for grants to be issued.)"
	},
	"filters": {
		"title": "Filters",
		"showFilters": "Show filters",
		"hideFilters": "Hide filters",
		"identity": "Identity",
		"modeUser": "User",
		"modeVirtualKey": "Virtual key",
		"modeSession": "Session",
		"virtualKey": "Virtual Key",
		"users": "Users",
		"searchVirtualKeys": "Search virtual keys",
		"searchUsers": "Search by name or email"
	},
	"revokeDialog": {
		"title": "Revoke this OAuth grant?",
		"description": "The refresh token for this grant stops rotating right away, so the MCP client can no longer renew its access. Its current access token is a short-lived JWT that keeps working on the <1>/mcp</1> endpoint until it expires (default 10 minutes), after which the client is fully cut off and must reconnect via the OAuth consent flow."
	}
}